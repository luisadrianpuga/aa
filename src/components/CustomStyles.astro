---
import '@fontsource/inter/variable.css';

// Nunito
// Dosis
---

<style is:inline is:global>
  :root {
    --aw-font-sans: 'InterVariable';
    --aw-font-serif: var(--aw-font-sans);
    --aw-font-heading: var(--aw-font-sans);

    --aw-color-primary: #D4AF37; /* subdued gold */
    --aw-color-secondary: #0D47A1; /* deep blue */
    --aw-color-accent: #D4AF37; /* subdued gold */
    --aw-color-text-page: #000000; /* black */
    --aw-color-text-muted: #999999; /* gray */
    --aw-color-bg-page: #FFFFFF; /* OLED Black (this will appear whiter on OLED displays) */
  }
</style>
